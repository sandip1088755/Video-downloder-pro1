<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Cleartext disabled by default: production downloads must be HTTPS -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>
